{
  "name": "cypress2024",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress-prod-test": "npx cypress run --spec cypress/e2e/login.cy.js --config-file prod.config.js",
    "cypress-stage-test": "npx cypress run --spec cypress/e2e/login.cy.js  --config-file stage.config.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.7.0",
    "cypress-file-upload": "^5.0.8",
    "npx": "^10.2.2"
  },
  "devDependencies": {
    "node-xlsx": "^0.24.0"
  },
  "description": ""
}
